Problema  
	=> Solucion

1. No puedo montar máquinas virtuales con Docker:
	=> Verificar settings de virtualización en BIOS

2. El comando compose no se ejecuta:
	=> Abrir DockerDesktop y cmd como Administrador

3. Errores de composicion por conflictos en librerias:
	=> Limpiar cache de docker y reiniciar ordenador
	=> Usar versión de GitHub, no la de desarrollo local.
	=> Checkear version de node de docker compose si alguna vez en desarrollo usaron npm update

4. Errores menores durante ejecución de compose:
	=> Pueden no ser relevantes

5. Servicios no inician o no funcionan como deberian:
	=> Checkear logs de composicion
	=> Checkear logs de imagenes
	=> Checkear consola de desarrollador en brower; Buscar errores o respuestas anomalas.
        => Checkear configuracion de posible lista de achivos culpables.

6. Lista de posibles culpables:

- DockerFile
- docker-compose.yml
  => mirar puertos y varibles de entorno
- server.js en Backend(y otros depedendiendo del error, pero principlamente ese)
- main en Backend(y otros depedendiendo del error, pero principlamente ese)
- archivos app en backend, en el directorio del main de backend
- .env en frontend y backend
- componentes del frontend
- nginx configuration